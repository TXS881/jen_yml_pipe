pipeline:
  agent:
    any:
  stages:
    - stage: "Test"
      steps:
        - sh "pwd"
        - sh "rm -rf pwd/"
        - sh "cd pwd"
        - sh "git clone https://github.com/TXS881/jen_yml_pipe.git"
        - sh "cd jen_yml_pipe"
        - sh "git branch bran1"
        - sh "ls -ltr"
        - echo "run second script"
    - stage: "Prod"
      steps:
        - echo "run first script"
        - echo "run second script for prod"